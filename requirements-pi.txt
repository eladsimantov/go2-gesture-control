# Python requirements for Raspberry Pi 4 (ARM64)
# Excludes Windows-specific and unused dependencies to prevent build/installation failures.

# Pin numpy to <2.0.0 because mediapipe and older wheels are not compatible with numpy 2.x C-API changes.
numpy>=1.20.0,<2.0.0

# OpenCV is required for capturing camera frames and rendering UI overlays.
opencv-python>=4.8.0

# MediaPipe is required for real-time hand-gesture recognition.
mediapipe>=0.10.0
